schema_version = 1
name = "entry-performance-lines"
version = "0.1"
scope = "generic"
priority = 10
enabled = true
description = "Load the generic entry-count throughput line extractor for English entry lines and Chinese 条目/表项/入口 performance lines."

[match]
projects = ["*"]

[outputs]
artifact_type = "benchmark_matrix"
benchmark_family = "generic-performance"
extractor = "generic-entry-performance-lines"
